require 'pry'

def nyc_pigeon_organizer(data)
  master_hash = {}
  data.each { |outer_key, outer_value| 
    outer_value.each { |inner_key, outer_key|
      
    
    if value.class == String  
      master_hash[key] = value
    end 
# master_hash
  } }
end
